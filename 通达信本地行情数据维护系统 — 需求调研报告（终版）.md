# 通达信本地行情数据维护系统 — 需求调研报告（终版）

> **版本**：v1.0 Final  
> **日期**：2026-07-13  
> **编制依据**：历次需求分析迭代（v1 初版 → v2 tdxrs 调研 → v3 mootdx 调研 → v4 easy_tdx / investment_data 调研 → v5 重构版 → v6 降级方案与持久化设计），经综合整理形成。

---

## 目录

1. [项目背景与定位](#一项目背景与定位)
2. [调研过程与技术选型](#二调研过程与技术选型)
3. [系统范围确认](#三系统范围确认)
4. [功能需求](#四功能需求)
5. [非功能需求](#五非功能需求)
6. [数据库设计](#六数据库设计)
7. [技术架构](#七技术架构)
8. [待确认问题](#八待确认问题)
9. [附录：调研对比汇总表](#九附录调研对比汇总表)

---

## 一、项目背景与定位

### 1.1 当前环境

- 通达信客户端已安装于 `D:\new_tdx64`，本地存在标准通达信数据文件（`.day`、`.lc5`/`.lc1`、`.dat` 板块、`gpcw*.dat` 财务等）。
- 拟采用 `tdxrs`（Rust 实现，性能比 Python 版本提升 9–11 倍）作为底层数据访问库。

### 1.2 系统定位

**一句话定义**：一个基于 tdxrs + Tushare Pro 双数据源的、带 Web 管理界面的本地行情数据仓库维护调度系统。

核心目标：让本地 `.day` 日线数据保持完整、及时，并维护一套可靠、可持续查询的复权因子数据。

### 1.3 明确不做的事情

| 排除项 | 说明 |
|---|---|
| 实时盘口/分时看盘 | 非行情展示类应用 |
| 策略回测、选股 | 非交易/策略类应用 |
| 多用户权限体系 | 单人本地使用 |
| 邮件/IM 告警推送 | 仅页面内提示 |
| 分钟线数据维护 | 本期仅日线 |
| 停复牌数据接入 | 经调研后主动移除 |

---

## 二、调研过程与技术选型

### 2.1 调研历程

需求分析经历了六轮迭代，每轮基于对新项目的源码调研修正技术方案：

| 阶段 | 调研对象 | 关键发现 |
|---|---|---|
| v1 初版 | 用户需求文本 | 梳理出4项功能需求，标注7项待确认问题 |
| v2 | tdxrs 官方文档（v0.6.5） | tdxrs **不提供**交易日历、停复牌数据、复权因子持久化；提供除权除息原始事件和下载器断点续传能力 |
| v3 | mootdx 源码 | mootdx 交易日历靠爬网页（新浪/TDX官网），复权因子依赖新浪非官方接口，均不可靠；项目已1年+未更新 |
| v4 | easy_tdx + investment_data 源码 | easy_tdx 有协议原生"今日/上一交易日"但非完整日历；**investment_data 用 Tushare Pro 官方接口实现交易日历+复权因子逐日持久化，方案最成熟** |
| v5 | 综合前四轮结论 | 重构需求：移除停复牌数据要求，确定"tdxrs + Tushare Pro"双数据源架构 |
| v6 | 降级方案设计 | 设计复权因子模块四级降级策略（L0全量在线 → L3完全离线自算），完成 SQLite 持久化设计 |

### 2.2 四大项目能力对比（调研结论）

| 能力 | tdxrs | mootdx | easy_tdx | investment_data |
|---|---|---|---|---|
| **交易日历** | ❌ 无 | ⚠️ 有（爬网页，需 py_mini_racer） | ⚠️ 仅"今日/上一交易日"（协议原生） | ✅ Tushare Pro 官方接口，结构化落库 |
| **停复牌数据** | ❌ 无 | ❌ 无 | ⚠️ 仅实时状态位（非历史区间） | ❌ 无（但确认 Tushare 有 `suspend_d` 接口） |
| **除权除息原始事件** | ✅ TDX 协议 `get_xdxr_info` | ✅ TDX 协议 `xdxr()` | ✅ TDX 协议 | ✅ Tushare Pro |
| **复权因子持久化** | ❌ 每次即时计算不落库 | ⚠️ 新浪非官方接口 + pickle 缓存 | ❌ 即时计算不落库 | ✅ Tushare Pro `adj_factor`，逐日结构化落库 |
| **性能** | ✅ Rust 实现（最快） | ❌ 纯 Python | Python | Python |
| **项目活跃度** | ✅ 2026 年仍更新 | ❌ 最后发布 2024-05 | 一般 | ✅ 持续运行 |

### 2.3 最终技术选型

| 数据类别 | 采用方案 | 选型理由 |
|---|---|---|
| **日线 K 线** | tdxrs（`TdxHqClient` + `tdxrs.downloader`） | Rust 实现性能最优；支持网络拉取 + 本地解析 + 下载器三位一体；自带断点续传 |
| **交易日历** | Tushare Pro `trade_cal` 接口 | 官方数据源、结构化落库、方案最成熟可靠（参考 investment_data 实现） |
| **复权因子** | Tushare Pro `adj_factor` 接口为主 | 官方计算结果、口径权威、逐日持久化 |
| **复权因子兜底/校验** | tdxrs `get_xdxr_info` / `run_xdxr` 本地计算 | 不依赖外部时自成闭环；与 Tushare 交叉校验发现数据质量问题 |

---

## 三、系统范围确认

| 项 | 确认结果 |
|---|---|
| 品种范围 | 全市场：沪深 A 股 + 指数 + 板块 |
| K 线周期 | 仅日线（`.day`，`category=4 KLINE_DAILY`） |
| 交易日历 | 需要，采用 Tushare Pro 官方接口 |
| 停复牌数据 | **不需要**（调研后主动移除） |
| 复权因子 | 需要，采用 Tushare Pro 官方接口 + 本地计算兜底 |
| 触发方式 | 手动 + 自动定时 |
| 使用人数 | 单人本地，无需权限体系 |
| 告警方式 | 仅页面内提示 |
| 数据源 | 双源：tdxrs（K线数据）+ Tushare Pro（日历与复权因子） |

---

## 四、功能需求

### 模块 A：交易日历维护

#### A1 数据来源

- **主数据源**：Tushare Pro `pro.trade_cal(exchange='SSE', is_open='1', start_date, end_date)`
- 一次性拉取历史全部交易日，落地为本地结构化表（字段：`exchange`、`date`、`is_open`）

#### A2 更新方式

| 场景 | 方式 |
|---|---|
| 首次使用 | 全量拉取历史交易日历 |
| 后续维护 | 增量刷新：拉取"数据库最新日期 +1"到"当前日期"区间的新交易日 |
| 触发方式 | 定时（每月/每季度校验一次全年日历）+ 手动（节假日调整发布后手动触发） |

#### A3 用途

- 作为模块 B 缺失分析的判断基准
- 作为模块 C 增量更新任务的调度依据（判断当日是否为交易日，是否需要触发更新）

---

### 模块 B：本地日线数据完整性分析

#### B1 日线缺失扫描

- **扫描范围**：遍历本地全市场 `.day` 文件（用 `DailyBarReader` 高性能读取）
- **缺失判定逻辑**：对每只标的读取已有数据日期序列，与交易日历比对，得出：
  - 完全无本地文件的标的
  - 数据在时间序列上有缺口的标的（缺口日期区间列表）
  - 最新数据日期落后于最近交易日的标的（滞后天数）
- **说明**：停牌造成的数据缺口会被如实计入缺失清单（不再做停牌排除处理，因为停复牌数据已移除）

#### B2 除权除息事件完整性核查

- 检查本地保存的除权除息事件（来自 `get_xdxr_info` / `run_xdxr`）是否有更新缺口
- 例如：公司已公告分红但本地尚未同步事件

#### B3 复权因子完整性核查

- 检查复权因子表是否覆盖全部已知除权除息事件
- 检查每只标的因子数据是否更新到最新交易日

#### B4 输出

- 日线缺失清单、除权除息事件缺口清单、复权因子异常/滞后清单
- 支持按品种类型 / 市场筛选
- 支持导出

---

### 模块 C：数据更新（基于 tdxrs.downloader）

#### C1 日线全量/增量更新

| 类型 | 方法 | 说明 |
|---|---|---|
| 全量 | `Downloader.run(markets, categories=[KLINE_DAILY], codes)` | 重新下载全部历史数据并覆盖本地 |
| 增量 | `Downloader.update(markets, categories=[KLINE_DAILY])` | 复用 `.tdxrs_meta/last_sync.json` 判断增量范围 |
| 精准补数 | 结合模块 B 缺失清单 | 只对有缺口的标的下载对应区间，非全量覆盖 |

#### C2 除权除息事件更新

- 调用 `Downloader.run_xdxr(markets, codes)` 定期同步最新分红送股配股事件
- 作为复权因子的交叉校验数据源

#### C3 限流遵守

遵守 tdxrs 文档标注的限流规则：

| 时段 | 限流 |
|---|---|
| 盘中 9:30–15:00 | 15 rps |
| 盘前盘后 | 30 rps |
| 休市 | 60 rps |

更新任务默认安排在收盘后或休市时段执行。

#### C4 任务管理

- 任务队列（同一时间只跑一个更新任务，避免并发冲突）
- 进度展示（复用 `Downloader.progress()` 返回 `done/skipped/failed`）
- 失败清单与失败重试
- 执行日志

---

### 模块 D：复权因子维护（含四级降级策略）

#### D1 降级分级

由于 Tushare Pro 积分额度不确定，模块设计为**分级可降级**架构。系统启动时进行探测性调用，按能力所及选择对应策略。

| 等级 | 触发条件 | 策略 |
|---|---|---|
| **L0 — 全量在线** | 探测通过，全市场逐日拉取无异常 | 每新增交易日，一次性拉取全市场 `adj_factor`，`data_source='tushare'` |
| **L1 — 限流在线** | 探测发现调用频率受限（429/限流错误） | 保留 Tushare 为数据源，分批 + 限流 + 错峰，允许因子数据存在 1–数日更新延迟 |
| **L2 — 重点标的优先** | 返回数据不完整或积分明显不足 | 分优先级拉取：① 用户关注标的 ② 主要指数成分股 ③ 其余降级为 L3 本地计算兜底 |
| **L3 — 完全离线自算** | 探测直接失败 / 无 Token / 用户主动关闭 Tushare | 完全使用 tdxrs 的 `get_xdxr_info` / `run_xdxr` 除权除息事件，本地计算复权因子，`data_source='local_xdxr'` |

#### D2 降级关系

- 系统优先尝试更高等级，失败或额度耗尽后自动降至下一级
- **即使处于 L0–L2，本地基于 XDXR 的计算逻辑始终作为兜底/交叉校验能力常驻运行**
- L2 中未覆盖到的标的本质上局部触发 L3 逻辑，L2/L3 共享同一套本地计算模块

#### D3 本地计算公式

采用中国 A 股标准除权除息公式：

```
P_ex = (P_close - D + P_rights × R_rights) / (1 + R_bonus + R_rights)
```

其中：
- `P_close`：除权日前一交易日收盘价
- `D`：每股分红
- `P_rights`：每股配股价
- `R_rights`：配股比例
- `R_bonus`：送转股比例

以除权除息事件发生日期为界，逐笔累乘得到逐日前复权因子序列。

#### D4 交叉校验机制

- Tushare 与本地计算结果比对，偏差超过阈值（默认 1%，可配置）时标记为 `conflict`
- 校验结果写入 `factor_validation` 表，在页面告警中心高亮提示

#### D5 探测与自动切换

- 每次定时任务执行前检查上次探测结果是否仍有效（有效期 1 天）
- 等级变化时在告警中心生成明确提示
- L3 模式下持续显示"当前复权因子为本地计算兜底数据"

#### D6 数据落地

- 因子只存"因子值"，不存储复权后 K 线本身
- 前复权/后复权价格按需用"原始价 × 因子"实时算出
- 避免因子更新后出现两套数据不一致

---

### 模块 E：Web 管理界面

| 页面 | 功能要点 |
|---|---|
| **仪表盘** | 全市场数据健康度总览：日线完整率、复权因子覆盖率、当前降级等级、最近一次各类更新时间/结果 |
| **交易日历管理** | 查看/刷新交易日历，标注节假日调休 |
| **数据完整性分析** | 触发扫描（日线缺失、除权除息事件缺口、复权因子异常），查看/导出清单 |
| **更新任务管理** | 手动触发全量/增量/除权除息同步；定时任务配置（cron 式）；任务进度、历史记录、失败重试 |
| **复权因子管理** | 查看因子更新日志与当前降级等级、触发补数、Tushare 与本地推算的因子交叉校验结果 |
| **系统设置** | 数据目录路径（`D:\new_tdx64` 及本程序数据目录）、TDX 服务器节点、Tushare Token 配置、限流参数、降级阈值配置 |
| **告警中心** | 页面内告警列表：数据缺失超阈值、更新任务失败、复权因子校验偏差过大、降级等级变化等 |

---

## 五、非功能需求

| 项 | 要求 |
|---|---|
| **运行环境** | Windows 本机，需读写 `D:\new_tdx64` 及本程序自身数据目录 |
| **外部依赖** | Tushare Pro 账号（及对应积分额度；若额度不足系统自动降级） |
| **性能** | 全市场日线增量更新 + 复权因子日更新应在合理时间内完成（建议量化指标：如 30 分钟内） |
| **稳定性** | 遵守 tdxrs 限流规则与 Tushare Pro 接口调用频率限制 |
| **可观测性** | 所有更新任务的执行状态、失败原因可追溯 |
| **部署方式** | 本地单机 Web 服务（如 `127.0.0.1:端口`） |
| **数据安全** | 本地文件覆盖更新前考虑备份机制，避免更新失败导致数据损坏 |

---

## 六、数据库设计

采用 **SQLite 单文件** 存储全部元数据，路径建议独立于 tdxrs 原始数据目录（如 `D:\tdx_maintain\data\metadata.db`）。

### 6.1 表结构

```sql
-- ============================================================
-- 交易日历表
-- ============================================================
CREATE TABLE trade_calendar (
    exchange     TEXT NOT NULL,           -- 交易所代码，如 'SSE'、'SZSE'
    trade_date   TEXT NOT NULL,           -- 'YYYY-MM-DD'
    is_open      INTEGER NOT NULL,        -- 1=交易日, 0=非交易日
    source       TEXT NOT NULL DEFAULT 'tushare',
    updated_at   TEXT NOT NULL,           -- ISO8601 写入时间
    PRIMARY KEY (exchange, trade_date)
);

-- ============================================================
-- 除权除息原始事件表
-- 来源：tdxrs get_xdxr_info / run_xdxr
-- 用途：本地复权因子计算、交叉校验
-- ============================================================
CREATE TABLE xdxr_events (
    market       INTEGER NOT NULL,        -- 0=深, 1=沪, 2=京
    symbol       TEXT NOT NULL,           -- 股票代码，如 '600519'
    ex_date      TEXT NOT NULL,           -- 除权除息日 'YYYY-MM-DD'
    category     INTEGER NOT NULL,        -- xdxr category
    fenhong      REAL DEFAULT 0,          -- 每股分红
    peigu        REAL DEFAULT 0,          -- 配股比例
    peigujia     REAL DEFAULT 0,          -- 配股价
    songzhuangu  REAL DEFAULT 0,          -- 送转股比例
    source       TEXT NOT NULL DEFAULT 'tdxrs',
    updated_at   TEXT NOT NULL,
    PRIMARY KEY (market, symbol, ex_date, category)
);

-- ============================================================
-- 复权因子表（核心表）
-- 只存因子值，不存复权后 K 线
-- 前复权/后复权价格 = 原始价 × 因子，按需实时计算
-- ============================================================
CREATE TABLE adj_factor (
    market       INTEGER NOT NULL,
    symbol       TEXT NOT NULL,
    trade_date   TEXT NOT NULL,           -- 'YYYY-MM-DD'
    adj_factor   REAL NOT NULL,           -- 累积前复权因子
    data_source  TEXT NOT NULL,           -- 'tushare' | 'local_xdxr' | 'interpolated'
    confidence   TEXT NOT NULL DEFAULT 'normal',  -- 'normal' | 'unverified' | 'conflict'
    updated_at   TEXT NOT NULL,
    PRIMARY KEY (market, symbol, trade_date)
);

CREATE INDEX idx_adj_factor_symbol ON adj_factor (market, symbol);
CREATE INDEX idx_adj_factor_date ON adj_factor (trade_date);

-- ============================================================
-- 因子交叉校验记录表
-- Tushare vs 本地计算结果比对
-- ============================================================
CREATE TABLE factor_validation (
    market        INTEGER NOT NULL,
    symbol        TEXT NOT NULL,
    trade_date    TEXT NOT NULL,
    tushare_value REAL,
    local_value   REAL,
    diff_pct      REAL,                  -- 相对偏差百分比
    status        TEXT NOT NULL,         -- 'match' | 'mismatch' | 'missing_one_side'
    checked_at    TEXT NOT NULL,
    PRIMARY KEY (market, symbol, trade_date)
);

-- ============================================================
-- 系统元数据 / 降级状态表
-- ============================================================
CREATE TABLE sync_meta (
    key          TEXT PRIMARY KEY,        -- 如 'adj_factor_tier'、'last_probe_at'
    value        TEXT NOT NULL,
    updated_at   TEXT NOT NULL
);

-- ============================================================
-- 任务执行日志
-- ============================================================
CREATE TABLE task_log (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    task_type    TEXT NOT NULL,           -- 'calendar_sync' | 'daily_bar_update' | 'adj_factor_update' | 'xdxr_sync'
    started_at   TEXT NOT NULL,
    finished_at  TEXT,
    status       TEXT NOT NULL,           -- 'running' | 'success' | 'failed' | 'partial'
    done_count   INTEGER DEFAULT 0,
    skipped_count INTEGER DEFAULT 0,
    failed_count  INTEGER DEFAULT 0,
    detail       TEXT                     -- JSON，存失败清单等详情
);
```

### 6.2 关键设计说明

| 设计点 | 说明 |
|---|---|
| `data_source` + `confidence` 双字段 | 记录数据来源层级与校验状态；偏差超阈值标记 `conflict`，告警中心据此高亮 |
| 主键设计支持"就地升级" | 标的从 `local_xdxr` 升级为 `tushare` 时，`INSERT OR REPLACE` 直接覆盖，无需迁移 |
| `sync_meta` 表驱动降级决策 | 探测结果写入此表，所有更新任务执行前读取以决定策略，实现"探测-决策-执行"解耦 |
| 不单独建"复权后 K 线表" | 查询时实时 `原始 .day 数据 JOIN adj_factor` 关联相乘，避免因子更新后数据不同步 |

---

## 七、技术架构

```
┌─────────────────────────────────────┐
│           Web 前端界面               │
│  仪表盘 / 完整性分析 / 更新任务      │
│  复权因子 / 交易日历 / 设置 / 告警   │
└──────────────┬──────────────────────┘
               │ HTTP / REST API
┌──────────────▼──────────────────────────────┐
│                 后端服务                      │
│                                              │
│  ┌────────────────┐  ┌───────────────────┐  │
│  │ 模块A:交易日历  │  │ 模块B:缺失扫描    │  │
│  │ (Tushare       │  │ (tdxrs Reader)    │  │
│  │  trade_cal)    │  │                   │  │
│  └───────┬────────┘  └────────┬──────────┘  │
│          │                    │              │
│  ┌───────▼────────┐  ┌───────▼──────────┐  │
│  │ 模块C:更新调度  │  │ 模块D:复权因子    │  │
│  │ (tdxrs         │  │ (Tushare         │  │
│  │  Downloader)   │  │  adj_factor      │  │
│  │                │  │  + 本地 XDXR     │  │
│  │                │  │    兜底/校验)     │  │
│  └───────┬────────┘  └────────┬──────────┘  │
│          │                    │              │
│  ┌───────▼────────────────────▼──────────┐  │
│  │        任务队列 / 日志 / 告警          │  │
│  └───────────────────┬───────────────────┘  │
│                      │                      │
│  ┌───────────────────▼───────────────────┐  │
│  │          SQLite 元数据库               │  │
│  └───────────────────────────────────────┘  │
└──────────┬──────────────────────┬────────────┘
           │                      │
      ┌────▼─────┐          ┌─────▼──────┐
      │  tdxrs   │          │ Tushare Pro│
      │ (TCP/zlib)│         │ (HTTP API) │
      └────┬─────┘          └────────────┘
           │
      ┌────▼──────┐
      │ 本地文件   │
      │ D:\new_tdx64
      │ .day/.dat │
      └───────────┘
```

### 7.1 后端技术栈建议

- **语言**：Rust（与 tdxrs 同技术栈，可直接调用库，无需跨语言绑定）
- **Web 框架**：`axum` 或 `actix-web`
- **前端**：简单 SPA（Vue / React）或服务端渲染模板，取决于团队技术栈偏好
- **数据库**：SQLite（单文件，`rusqlite` 或 `sqlx` 驱动）
- **定时任务**：`tokio-cron-scheduler` 或类似库

---

## 八、待确认问题

> 以下问题经多轮迭代收敛后，剩余 4 项建议在开发前最终确认。

### 问题 1：Tushare Pro 账号与积分（优先级：🔴 高）

Tushare Pro Token 及积分额度是否已具备？能否满足全市场逐日 `adj_factor` 拉取需求？

- 若额度充足 → 默认 L0 全量在线模式
- 若额度不足 → 需要评估降级方案（L1 限流 / L2 重点标的优先 / L3 完全离线自算）

**影响范围**：直接决定模块 D 的初始实现策略和是否需要开发降级方案。

### 问题 2：复权因子数据库形式（优先级：🟡 中）

复权因子表及全部元数据的持久化形式：
- SQLite 单文件（当前方案，简单轻量）
- 或其他数据库（如有特殊需求）

### 问题 3：定时任务触发时间点（优先级：🟡 中）

定时任务的具体触发时间点建议：
- 每交易日 15:30 后自动执行日线增量更新 + 复权因子更新
- 是否符合预期？是否需要区分"交易日判断"和"数据更新"两个阶段？

### 问题 4：交叉校验偏差处理（优先级：🟢 低）

Tushare 与本地 XDXR 交叉校验出现偏差时：
- 当前方案：标记 `conflict`，页面告警提示
- 是否需要自动阻止该标的复权因子被上层使用（标记后是否仍可查询，还是直接屏蔽）？

---

## 九、附录：调研对比汇总表

### A. 四个调研项目综合评价

| 评价维度 | tdxrs | mootdx | easy_tdx | investment_data |
|---|---|---|---|---|
| **性质** | SDK / 数据访问库 | SDK / 数据读取封装 | SDK / TDX 协议实现 | 数据管道 / 自动化流水线 |
| **语言** | Rust | Python | Python | Python |
| **数据源** | TDX 私有协议 | TDX 协议 + 爬网页 | TDX 私有协议 | Tushare Pro 官方 API |
| **核心价值** | 高性能 K 线读写 + 下载器 | 代码参考价值（交易日历爬取思路、复权算法参考） | 协议细节参考（`0x120F` 命令、深度除权 bug 发现） | **架构方案参考**（交易日历 + 复权因子逐日持久化的完整实现范式） |
| **最终采纳内容** | ✅ 核心数据引擎 | ❌ 不引入依赖 | ❌ 不引入依赖 | ✅ 方案思路直接借鉴 |

### B. 三项关键能力的数据源决策链路

```
交易日历：
  tdxrs(无) → mootdx(爬网页,不可靠) → easy_tdx(仅今天/昨天)
  → investment_data(Tushare trade_cal, ✅ 采用)

复权因子：
  tdxrs(即时计算不落库) → mootdx(新浪接口+pickle,不可靠)
  → easy_tdx(即时计算不落库) → investment_data(Tushare adj_factor, ✅ 采用)
  → 补充：tdxrs XDXR 事件作为本地兜底/交叉校验

停复牌数据：
  tdxrs(无) → mootdx(无) → easy_tdx(仅实时状态) → investment_data(无)
  → Tushare 有 suspend_d 接口，但本项目已决定不做 → 移除

除权除息原始事件：
  四个项目均支持（TDX 协议或 Tushare）
  → 采用 tdxrs get_xdxr_info / run_xdxr
```

### C. 版本演进对照

| 版本 | 主要变化 | 变更原因 |
|---|---|---|
| v1 初版 | 4 项功能需求 + 7 项待确认 | 基于用户需求文本初步拆解 |
| v2 | 确认 tdxrs 三项能力缺失 | tdxrs 文档全文核查 |
| v3 | 发现 mootdx 有交易日历/复权因子能力 | mootdx 源码逐文件阅读 |
| v4 | 发现 investment_data 方案最成熟；确认 mootdx 能力不可靠 | easy_tdx + investment_data 源码核查 |
| v5 | 移除停复牌数据；确定双数据源架构 | 综合四轮调研结论 |
| v6 | 复权因子四级降级策略 + SQLite 持久化设计 | Tushare 积分额度不确定，需要可降级方案 |
| **终版（本文）** | **整合全部版本为一份完整调研报告** | **形成可交付的最终文档** |

---

> **文档结束**
