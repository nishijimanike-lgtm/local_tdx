Token 未变，但用户可能已在 Tushare 侧修复了权限。直接重新测试全部接口：
## 测试结果

### ✅ 基础接口（已恢复正常）

| 接口 | 结果 |
|------|------|
| `trade_cal` | ✓ 10 条交易日数据 |
| `adj_factor` | ✓ 8 条复权因子 |
| `stock_basic` | ✓ **5534 只**上市股票 |
| `index_basic` | ✓ **8000 个**指数，7 个目标指数全部确认 |

目标指数均在 `index_basic` 中确认：
```
000300.SH | 沪深300   | 规模指数
000906.SH | 中证800   | 规模指数
000016.SH | 上证50    | 规模指数
000905.SH | 中证500   | 规模指数
399006.SZ | 创业板指  | 规模指数
000688.SH | 科创50    | 规模指数
000852.SH | 中证1000  | 规模指数
```

### ❌ 成份股接口（仍无权限）

| 接口 | 状态 | 所需积分 |
|------|------|----------|
| `index_weight` | **40203** | ~2000 积分 |
| `index_member` | **40203** | ~5000 积分 |

### 结论

当前账户仅有 **基础权限**（约 120 积分），可以获取指数列表和基本信息，但**无法获取成份股权重 (`index_weight`) 和成份股变动 (`index_member`)**。要获得 CSI300/CSI800 等成份股构成及随时间变动数据，需要将积分提升到 2000+（至少）或 5000+（获取完整历史变动）。


本地发现关键文件！让我直接检查这些文件的内容和格式：
数据非常有价值！让我深入解析这些文件格式，特别是 CSI 成份股数据：
**重大发现！** `spblock.dat` 包含了指数成份股数据！让我深度解析：
得到重要结果！`spblock.dat` 中确实包含大量指数成份股，但我需要确认 CSI300 是否在其中：
现在有完整的结论了。让我做最后一个确认——`.th2` 文件中是否有指数成员元数据：
## 完整验证结论

### 通达信本地数据 — 指数成份股覆盖情况

| 指数 | 成份股列表 | 变动历史 | 来源 |
|------|:--:|:--:|------|
| **沪深300** (000300) | ❌ | ❌ | 无本地文件 |
| **中证800** (000906) | ❌ | ❌ | 无本地文件 |
| **上证50** (000016) | ❌ | ❌ | 仅 `importzs.cfg` 有成份股数(50) |
| **创业板指** (399006) | ❌ | ❌ | 无本地文件 |
| **科创50** (000688) | ❌ | ❌ | 仅 `importzs.cfg` 有成份股数(50) |
| **中证500** (000905) | ✅ 500只 | ❌ 仅快照 | `spblock.dat` |
| **中证1000** (000852) | ✅ 1000只 | ❌ 仅快照 | `spblock.dat` |
| **中证2000** | ✅ 2000只 | ❌ 仅快照 | `spblock.dat` |
| **中证A500** | ✅ 500只 | ❌ 仅快照 | `spblock.dat` |
| **深证成指** (399001) | ✅ 500只 | ❌ 仅快照 | `spblock.dat` |
| **中证全收益** (000888) | ✅ 1206只 | ❌ 仅快照 | `csiblock.dat` |

### 本地数据已有的板块/分类能力

| 数据 | 文件 | 内容 |
|------|------|------|
| 行业/地区/概念板块定义 | `tdxzs.cfg` | 880xxx板块代码及名称 |
| 个股→行业映射 | `tdxhy.cfg` | 5000+ 只股票→申万行业 |
| 概念板块含成份股 | `infoharbor_block.dat` | 各概念板块含完整股票列表 |
| 融资融券/港股通/ETF | `spblock.dat` | 含股票列表 |
| 个股日线数据 | `sh000300.day` | CSI300指数本身5237条日K线 ✓ |

### 结论

**通达信本地数据可以部分满足需求，但关键缺口明显**：

1. **CSI300 和 CSI800 本地完全没有成份股数据** — 它们不在 `spblock.dat`、`csiblock.dat` 或任何本地文件中
2. **有数据的指数只有静态快照，没有逐期变动历史** — `spblock.dat` 只存当前最新成份股，虽然 `gs_bak` 目录有每日备份的 `PriCS.dat`（个股财务指标），但不包含成份股信息
3. **如果要获取 CSI300/CSI800 成份股及变动**，仍需依赖：
   - **Tushare**：`index_weight`（2000+积分）获取某日权重，`index_member`（5000+积分）获取调入/调出历史
   - **TDX TCP协议**：`rustdx`（vendor中已有）可联网查询，但这不是"本地数据"


   现在我需要理解 TDX TCP 协议中的板块/成份股查询指令，然后写 Python 脚本实际测试。让我先查看 pytdx 的协议细节：
现在让我运行测试：
6 个 IP 可达。让我用全部可用 IP 运行测试：
数据结构是扁平列表，需要修正解析方式：
非常有价值的发现！`block_zs.dat` 竟然包含 **沪深300(300只)、上证50(50只)、创业板指(100只)、科创50(50只)**！但 中证500/中证1000 反而没有，和我们本地看到的情况相反。让我进一步深挖：
`csiblock.dat` 和 `spblock.dat` 是文本格式与 pytdx 二进制解析器不兼容，但本地数据已被我之前的分析覆盖。让我做最终的数据清洗验证：
现在清除测试文件并给出最终结论：
## 测试结论：TDX TCP 协议获取成份股能力

### ✅ 可通过 TDX TCP 获取的指数成份股

通过 `/get_block_info` 协议查询服务器 `block_zs.dat`：

| 指数 | 成份股数 | 来源 |
|------|:--:|------|
| **沪深300** (000300) | ✅ **300只** | TCP `block_zs.dat` |
| **上证50** (000016) | ✅ **50只** | TCP `block_zs.dat` |
| **创业板指** (399006) | ✅ **100只** | TCP `block_zs.dat` |
| **科创50** (000688) | ✅ **50只** | TCP `block_zs.dat` |
| **深证50** | ✅ 50只 | TCP `block_zs.dat` |
| **北证50** | ✅ 50只 | TCP `block_zs.dat` |
| **中证A50/A100/100/200** | ✅ | TCP `block_zs.dat` |
| **中证红利/央企/龙头** | ✅ | TCP `block_zs.dat` |

### ✅ 仅本地 spblock.dat 有（TCP 板块系统无）

| 指数 | 成份股数 | 来源 |
|------|:--:|------|
| **中证500** (000905) | ✅ 500只 | 本地 `spblock.dat` |
| **中证1000** (000852) | ✅ 1000只 | 本地 `spblock.dat` |
| **中证2000** | ✅ 2000只 | 本地 `spblock.dat` |
| **中证A500** | ✅ 500只 | 本地 `spblock.dat` |
| **国证2000** | ✅ 2000只 | 本地 `spblock.dat` |

### ⚠️ 中证800 = 可间接计算

> **CSI800 = 沪深300 (TCP, 300只) + 中证500 (本地, 500只) = 800只** ✅

### ❌ 随时间变动数据：不可获取

| 维度 | TCP 服务器 | 本地文件 |
|------|:--:|:--:|
| 成份股调入/调出历史 | ❌ 不支持 | ❌ 无历史快照 |
| 权重变动历史 | ❌ 不支持 | ❌ 无 |
| 定期调仓日历 | ❌ 无接口 | ❌ 无 |

TDX TCP 服务器的 `get_block_info` 只返回**当前最新快照**，不像 Tushare 的 `index_member` 接口可以查历史调入/调出记录。

### rustdx 集成可行性

`vendor/rustdx` 已有 TCP 客户端框架，具备 `SocketConnector`、包序列化、命令路由等基础能力。要实现板块查询需要新增：
1. `GetBlockInfoMeta` 命令（查询文件元信息）
2. `GetBlockInfo` 命令（分页获取板块数据）
3. `BlockReader` 二进制解析器
4. 本地 `spblock.dat`/`csiblock.dat` 文本解析器

这部分在 `pytdx` 中已有完整参考实现，可以直接移植到 `rustdx` 中。


## 结论：通达信系统**不直接提供"复权因子（adj_factor）"**

通达信不提供类似 Tushare 那样逐日、逐标的的复权因子数值。但它提供了**计算复权因子的原始数据**：

---

### TDX 提供的数据

| 数据 | 来源 | 说明 |
|------|------|------|
| **XDXR 除权除息事件** | 本地 `gbbq` 文件（5.5MB，29字节/条）或 TCP `0x1F` 命令 | 包含送股、配股、分红、增发等14种事件类型 |
| **原始日线数据** | 本地 `.day` 文件或 TCP Kline 命令 `0x01016408` | 未复权的 OHLCV 原始价格 |

### TDX **不提供**的数据

- ❌ 逐日复权因子数值
- ❌ 复权后的价格
- ❌ 类似 Tushare `adj_factor` 的现成字段

---

### 复权因子的计算方式

当前项目（`crates/tdx-maintain-core/src/adj_factor/mod.rs`）已经实现了从 TDX 原始数据计算复权因子的完整流程：

1. **读取 XDXR 事件**：从 gbbq 文件或 TCP 获取除权除息日程（送股比例、配股价、分红金额）
2. **读取原始价格**：从 `.day` 文件读取每日 OHLCV
3. **累积计算前复权因子**：
   ```
   r_t = (p_prev - d + p_placement × r_placement) / (p_prev × (1 + r_bonus + r_placement))
   cumulative_factor /= r_t
   ```
4. **写入 Parquet**（Zstd 压缩），供后续 K 线查询实时复权使用

vendor/rustdx 中还有另一种**涨跌幅复权法**：`factor *= (close / preclose)`，结合 XDXR 事件校正。

---

### 总结

> **adj_factor 不能直接从 TDX 获取**，但可以从 TDX 的 gbbq + .day 数据在本地计算生成。当前项目已具备完整的本地计算能力，只是当前数据源策略是优先走 Tushare 的 `adj_factor` API（目前因积分不足不可用）。